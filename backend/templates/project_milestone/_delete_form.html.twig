<form method="post" action="{{ path('app_project_milestone_delete', {'id': project_milestone.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ project_milestone.id) }}">
    <button class="btn">Delete</button>
</form>
